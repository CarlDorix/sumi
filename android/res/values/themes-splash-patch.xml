<!--
  Patch for app/src/main/res/values/themes.xml — replace the existing
  Theme.Tachiyomi.SplashScreen block with this one. Only two lines change:
  the animated icon, and the animation duration the system holds it for.
-->
<style name="Theme.Tachiyomi.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenAnimatedIcon">@drawable/avd_launcher_icon</item>
    <item name="windowSplashScreenAnimationDuration">1000</item>
    <item name="windowSplashScreenBackground">@color/splash</item>
    <item name="postSplashScreenTheme">@style/Theme.Tachiyomi</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:windowLightStatusBar">true</item>
    <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
</style>

<!--
  And in values/colors.xml, point the splash background at the icon background
  so the mark sits on its own paper:

      <color name="splash">#FAFAFA</color>

  Note: windowLightStatusBar/windowLightNavigationBar flip to true above
  because the splash is now light. If you keep a dark @color/splash, leave
  them false and use the dark-mode icon colours instead.
-->
